<div class="float-md-right">
  <div class="btn-group">
    <%= link_to 'Voltar', mensagens_path, class: 'btn btn-secondary' %>
    <%= link_to 'Detalhar', mensagem_path(@mensagem), class: 'btn btn-secondary' %>
    <%= link_to 'Excluir', @mensagem, method: :delete, data: { confirm: 'Tem certeza?' }, class: 'btn btn-secondary' %>
  </div>
</div>

<h1>Editar Mensagem</h1>

<%= render 'form' %>
